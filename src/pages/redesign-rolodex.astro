---
import BaseLayout from "../layouts/BaseLayout.astro";
import RedesignRolodexPage from "../components/redesign-rolodex/RedesignRolodexPage.tsx";
const title = "Redesign Rolodex — stanwood.dev";
---

<BaseLayout
  title={title}
  description="Paste a URL and spin through alternate-universe redesigns. AI-generated design directions with concept cards, palettes, and ready-to-use prompts."
  slug="redesign-rolodex"
  favicon="/favicon-rr.svg"
  themeColor="#FAF6F0"
  fontsUrl="https://fonts.googleapis.com/css2?family=Permanent+Marker&family=Playfair+Display:wght@400;600&display=swap"
  schemaCategory="WebApplication"
  schemaName="Redesign Rolodex"
>
  <Fragment slot="head">
    <style is:global>
      /* ─── Redesign Rolodex Theme ─────────────────────────────── */
      :root {
        --rr-bg: #FAF6F0;
        --rr-card-bg: #FFFDF8;
        --rr-ink: #1C1917;
        --rr-gray: #57534E;
        --rr-light-gray: #A8A29E;
        --rr-border: #E7E0D8;
        --rr-border-dark: #D4CBC0;
        --rr-accent: #6D28D9;
        --rr-accent-light: #EDE9FE;
        --rr-accent-hover: #5B21B6;
        --rr-warm: #EA580C;
        --rr-warm-light: #FFF7ED;
      }

      body {
        font-family: 'Cabin', system-ui, sans-serif;
        background: var(--rr-bg);
        color: var(--rr-ink);
        margin: 0;
      }

      /* ─── Animations ─────────────────────────────────────────── */
      @keyframes rrFadeIn {
        from { opacity: 0; transform: translateY(12px); }
        to   { opacity: 1; transform: translateY(0); }
      }
      @keyframes rrFadeMsg {
        0%   { opacity: 0; transform: translateY(8px); }
        15%  { opacity: 1; transform: translateY(0); }
        85%  { opacity: 1; transform: translateY(0); }
        100% { opacity: 0; transform: translateY(-8px); }
      }
      @keyframes rrFloat {
        0%, 100% { transform: translateY(0) rotate(-2deg); }
        50% { transform: translateY(-8px) rotate(2deg); }
      }
      @keyframes rrFloat2 {
        0%, 100% { transform: translateY(0) rotate(3deg); }
        50% { transform: translateY(-6px) rotate(-1deg); }
      }
      @keyframes rrFloat3 {
        0%, 100% { transform: translateY(0) rotate(-1deg); }
        50% { transform: translateY(-10px) rotate(3deg); }
      }

      .rr-fade-in { animation: rrFadeIn 0.4s ease-out; }

      /* ─── Page Layout ────────────────────────────────────────── */
      .rr-page {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .rr-header {
        padding: 1.25rem 1.5rem;
        display: flex;
        align-items: center;
        gap: 1rem;
      }

      .rr-back {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        color: var(--rr-light-gray);
        text-decoration: none;
        font-size: 0.875rem;
        font-weight: 500;
        transition: color 0.15s;
      }
      .rr-back:hover { color: var(--rr-ink); }
      .rr-back svg { width: 16px; height: 16px; }

      .rr-brand {
        font-family: 'Space Mono', monospace;
        font-weight: 700;
        font-size: 1rem;
        color: var(--rr-accent);
        letter-spacing: -0.02em;
      }

      .rr-main {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 1rem 1.5rem 4rem;
      }

      .rr-app {
        width: 100%;
        max-width: 800px;
      }

      /* ─── Form / Idle ────────────────────────────────────────── */
      .rr-form-container {
        text-align: center;
        padding-top: 2rem;
      }

      .rr-title {
        font-family: 'Permanent Marker', cursive;
        font-size: 2.5rem;
        color: var(--rr-ink);
        margin: 0 0 0.25rem;
        letter-spacing: -0.02em;
        line-height: 1.1;
      }
      @media (min-width: 480px) {
        .rr-title { font-size: 3.5rem; }
      }

      .rr-subtitle {
        font-family: 'Space Mono', monospace;
        font-size: 0.85rem;
        color: var(--rr-light-gray);
        margin: 0 0 2.5rem;
      }

      .rr-form {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        max-width: 520px;
        margin: 0 auto;
      }
      @media (min-width: 480px) {
        .rr-form {
          flex-direction: row;
          gap: 0.5rem;
        }
      }

      .rr-input-wrap {
        flex: 1;
        position: relative;
        min-width: 0;
      }
      .rr-input {
        width: 100%;
        padding: 0.85rem 1rem;
        border: 2px solid var(--rr-border);
        border-radius: 10px;
        font-family: 'Space Mono', monospace;
        font-size: 1rem;
        color: var(--rr-ink);
        background: var(--rr-card-bg);
        transition: border-color 0.15s;
        box-sizing: border-box;
      }
      .rr-input:focus {
        outline: none;
        border-color: var(--rr-accent);
        box-shadow: 0 0 0 3px rgba(109, 40, 217, 0.1);
      }
      .rr-input::placeholder { color: var(--rr-border-dark); }
      .rr-ghost {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        padding: 0.85rem 1rem;
        font-family: 'Space Mono', monospace;
        font-size: 1rem;
        pointer-events: none;
        white-space: nowrap;
        overflow: hidden;
      }
      .rr-ghost-typed { visibility: hidden; }
      .rr-ghost-rest { color: var(--rr-border-dark); }

      .rr-btn-primary {
        padding: 0.85rem 1.75rem;
        border: 2px solid var(--rr-accent);
        border-radius: 10px;
        background: var(--rr-accent);
        color: #fff;
        font-family: 'Space Mono', monospace;
        font-size: 0.85rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.15s;
        white-space: nowrap;
      }
      .rr-btn-primary:hover {
        background: var(--rr-accent-hover);
        border-color: var(--rr-accent-hover);
      }
      .rr-btn-primary:active { transform: scale(0.98); }
      .rr-btn-primary:disabled { opacity: 0.3; cursor: not-allowed; }

      .rr-helper {
        font-size: 0.75rem;
        color: var(--rr-light-gray);
        margin: 1.5rem 0 0;
        font-style: italic;
      }

      /* ─── Mode Toggle ────────────────────────────────────────── */
      .rr-mode-toggle {
        display: flex;
        gap: 0;
        justify-content: center;
        margin: 1.5rem auto 0;
        max-width: 400px;
        border: 2px solid var(--rr-border);
        border-radius: 10px;
        overflow: hidden;
        background: var(--rr-card-bg);
      }

      .rr-mode-btn {
        flex: 1;
        padding: 0.6rem 0.75rem;
        border: none;
        background: transparent;
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        font-weight: 700;
        color: var(--rr-gray);
        cursor: pointer;
        transition: all 0.15s;
        text-transform: uppercase;
        letter-spacing: 0.02em;
      }
      .rr-mode-btn:not(:last-child) {
        border-right: 1.5px solid var(--rr-border);
      }
      .rr-mode-btn:hover { color: var(--rr-accent); }
      .rr-mode-active {
        background: var(--rr-accent);
        color: #fff !important;
      }

      /* ─── Examples ───────────────────────────────────────────── */
      .rr-examples {
        margin-top: 2rem;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        flex-wrap: wrap;
      }

      .rr-examples-label {
        font-family: 'Space Mono', monospace;
        font-size: 0.75rem;
        color: var(--rr-light-gray);
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      .rr-example-btn {
        padding: 0.3rem 0.7rem;
        border: 1.5px solid var(--rr-border);
        border-radius: 999px;
        background: transparent;
        color: var(--rr-gray);
        font-family: 'Space Mono', monospace;
        font-size: 0.75rem;
        cursor: pointer;
        transition: all 0.15s;
      }
      .rr-example-btn:hover {
        border-color: var(--rr-accent);
        color: var(--rr-accent);
        background: var(--rr-accent-light);
      }

      /* ─── Error ──────────────────────────────────────────────── */
      .rr-error {
        margin-top: 1.5rem;
        padding: 1rem 1.25rem;
        background: #fef2f2;
        border: 1.5px solid #fecaca;
        border-radius: 10px;
        text-align: center;
      }
      .rr-error p {
        color: #b91c1c;
        font-size: 0.85rem;
        margin: 0 0 0.75rem;
      }

      .rr-btn-secondary {
        padding: 0.5rem 1.25rem;
        border: 1.5px solid var(--rr-border);
        border-radius: 8px;
        background: var(--rr-card-bg);
        color: var(--rr-gray);
        font-family: 'Space Mono', monospace;
        font-size: 0.8rem;
        cursor: pointer;
        transition: all 0.15s;
      }
      .rr-btn-secondary:hover {
        border-color: var(--rr-accent);
        color: var(--rr-accent);
      }

      /* ─── Loading ────────────────────────────────────────────── */
      .rr-loading {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2rem;
        padding: 6rem 0;
        animation: rrFadeIn 0.3s ease-out;
      }

      .rr-loading-spinner {
        position: relative;
        width: 80px;
        height: 60px;
      }

      .rr-spinner-card {
        position: absolute;
        width: 52px;
        height: 36px;
        border-radius: 6px;
        border: 2px solid var(--rr-border-dark);
        background: var(--rr-card-bg);
        box-shadow: 0 2px 8px rgba(0,0,0,0.06);
      }
      .rr-sc-1 {
        top: 0; left: 6px;
        animation: rrFloat 2.4s ease-in-out infinite;
      }
      .rr-sc-2 {
        top: 6px; left: 16px;
        animation: rrFloat2 2.8s ease-in-out infinite;
        background: var(--rr-accent-light);
        border-color: var(--rr-accent);
      }
      .rr-sc-3 {
        top: 12px; left: 26px;
        animation: rrFloat3 2.2s ease-in-out infinite;
      }

      .rr-loading-text {
        font-family: 'Space Mono', monospace;
        font-size: 0.85rem;
        color: var(--rr-gray);
        text-align: center;
        min-height: 1.5em;
        animation: rrFadeMsg 2.2s ease-in-out;
      }

      /* ─── Result Layout ──────────────────────────────────────── */
      .rr-result-container {
        width: 100%;
      }

      .rr-result-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
        flex-wrap: wrap;
      }

      .rr-btn-reset {
        padding: 0.4rem 0.85rem;
        border: 1.5px solid var(--rr-border);
        border-radius: 8px;
        background: var(--rr-card-bg);
        color: var(--rr-gray);
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.15s;
        text-transform: uppercase;
      }
      .rr-btn-reset:hover {
        border-color: var(--rr-accent);
        color: var(--rr-accent);
      }

      .rr-result-url {
        font-family: 'Space Mono', monospace;
        font-size: 0.8rem;
        color: var(--rr-gray);
        flex: 1;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }

      .rr-result-count {
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        color: var(--rr-light-gray);
        background: var(--rr-accent-light);
        padding: 0.2rem 0.6rem;
        border-radius: 999px;
      }

      /* ─── Rolodex Viewer ─────────────────────────────────────── */
      .rr-rolodex-container {
        width: 100%;
      }

      .rr-rolodex-counter {
        text-align: center;
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        color: var(--rr-light-gray);
        margin-bottom: 0.75rem;
        letter-spacing: 0.05em;
      }

      .rr-rolodex-stage {
        position: relative;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.25rem;
      }

      .rr-nav-btn {
        flex-shrink: 0;
        width: 40px;
        height: 40px;
        border: 2px solid var(--rr-border);
        border-radius: 50%;
        background: var(--rr-card-bg);
        color: var(--rr-gray);
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.15s;
        z-index: 20;
      }
      .rr-nav-btn:hover {
        border-color: var(--rr-accent);
        color: var(--rr-accent);
        background: var(--rr-accent-light);
      }

      .rr-rolodex-track {
        position: relative;
        width: 100%;
        max-width: 480px;
        height: 520px;
        perspective: 1200px;
        overflow: hidden;
        touch-action: pan-x;
      }
      @media (max-width: 600px) {
        .rr-rolodex-track { height: 460px; max-width: 100%; }
      }

      .rr-rolodex-slot {
        position: absolute;
        top: 0;
        left: 50%;
        width: 420px;
        max-width: 94%;
        margin-left: -210px;
        height: 100%;
        transition: all 0.45s cubic-bezier(0.22, 1, 0.36, 1);
        transform-style: preserve-3d;
        transform-origin: center bottom;
      }
      @media (max-width: 480px) {
        .rr-rolodex-slot {
          width: 320px;
          margin-left: -160px;
        }
      }

      .rr-rolodex-dots {
        display: flex;
        justify-content: center;
        gap: 6px;
        margin-top: 1rem;
        flex-wrap: wrap;
        max-width: 100%;
      }

      .rr-dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
        border: 1.5px solid var(--rr-border-dark);
        background: transparent;
        cursor: pointer;
        padding: 0;
        transition: all 0.2s;
      }
      .rr-dot:hover { border-color: var(--rr-accent); }
      .rr-dot-active {
        background: var(--rr-accent);
        border-color: var(--rr-accent);
        transform: scale(1.3);
      }

      /* ─── Cards (shared) ─────────────────────────────────────── */
      .rr-card {
        background: var(--rr-card-bg);
        border: 2px solid var(--rr-border);
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 4px 24px rgba(28, 25, 23, 0.08), 0 1px 4px rgba(28, 25, 23, 0.04);
        height: 100%;
        display: flex;
        flex-direction: column;
      }

      .rr-card-number {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        font-weight: 700;
        color: var(--rr-light-gray);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        padding: 0.75rem 1rem 0;
      }

      .rr-card-label-tag {
        font-family: 'Space Mono', monospace;
        font-size: 0.65rem;
        font-weight: 700;
        color: var(--rr-accent);
        text-transform: uppercase;
        letter-spacing: 0.08em;
        padding: 0.25rem 1rem 0;
      }

      /* ─── Current Site Card ──────────────────────────────────── */
      .rr-card-screenshot {
        margin: 0.75rem;
        border-radius: 8px;
        overflow: hidden;
        border: 1px solid var(--rr-border);
        flex-shrink: 0;
      }
      .rr-card-screenshot img {
        width: 100%;
        height: auto;
        display: block;
        max-height: 220px;
        object-fit: cover;
        object-position: top;
      }

      .rr-card-info {
        padding: 0 1rem 1rem;
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
        overflow-y: auto;
      }

      .rr-card-title {
        font-family: 'Playfair Display', serif;
        font-size: 1.1rem;
        font-weight: 600;
        margin: 0;
        color: var(--rr-ink);
        line-height: 1.2;
      }

      .rr-card-desc {
        font-size: 0.8rem;
        color: var(--rr-gray);
        margin: 0;
        line-height: 1.45;
      }

      .rr-card-style-notes {
        display: flex;
        flex-wrap: wrap;
        gap: 0.35rem;
        margin-top: 0.25rem;
      }

      .rr-style-tag {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        padding: 0.2rem 0.5rem;
        border-radius: 4px;
        background: var(--rr-bg);
        border: 1px solid var(--rr-border);
        color: var(--rr-gray);
      }
      .rr-style-tag-accent {
        background: var(--rr-accent-light);
        border-color: var(--rr-accent);
        color: var(--rr-accent);
        font-weight: 700;
      }

      /* ─── Direction Card ─────────────────────────────────────── */
      .rr-card-concept {
        margin: 0.75rem;
        border-radius: 8px;
        overflow: hidden;
        border: 1px solid var(--rr-border);
        flex-shrink: 0;
        height: 200px;
        background: #f5f5f5;
      }

      .rr-concept-iframe {
        width: 400px;
        height: 280px;
        border: none;
        transform: scale(0.714);
        transform-origin: top left;
        pointer-events: none;
      }

      .rr-card-tagline {
        font-size: 0.8rem;
        color: var(--rr-gray);
        margin: 0;
        line-height: 1.4;
        font-style: italic;
      }

      .rr-card-palette {
        display: flex;
        gap: 4px;
        margin: 0.2rem 0;
      }

      .rr-swatch {
        width: 20px;
        height: 20px;
        border-radius: 4px;
        border: 1px solid rgba(0,0,0,0.1);
        flex-shrink: 0;
      }

      .rr-card-font-dir {
        font-family: 'Space Mono', monospace;
        font-size: 0.65rem;
        color: var(--rr-light-gray);
        margin: 0;
        line-height: 1.4;
      }

      /* ─── Copy Button ────────────────────────────────────────── */
      .rr-copy-btn {
        padding: 0.5rem 1rem;
        border: 1.5px solid var(--rr-accent);
        border-radius: 8px;
        background: transparent;
        color: var(--rr-accent);
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.15s;
        text-transform: uppercase;
        letter-spacing: 0.04em;
        margin-top: auto;
      }
      .rr-copy-btn:hover {
        background: var(--rr-accent);
        color: #fff;
      }
      .rr-copy-copied {
        background: #16a34a;
        border-color: #16a34a;
        color: #fff;
      }

      /* ─── More Controls ──────────────────────────────────────── */
      .rr-more-controls {
        display: flex;
        justify-content: center;
        gap: 0.5rem;
        margin-top: 1.5rem;
        flex-wrap: wrap;
      }

      .rr-more-btn {
        padding: 0.55rem 1.1rem;
        border: 2px solid var(--rr-border);
        border-radius: 10px;
        background: var(--rr-card-bg);
        color: var(--rr-gray);
        font-family: 'Space Mono', monospace;
        font-size: 0.75rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.15s;
      }
      .rr-more-btn:hover:not(:disabled) {
        border-color: var(--rr-accent);
        color: var(--rr-accent);
      }
      .rr-more-btn:disabled {
        opacity: 0.4;
        cursor: not-allowed;
      }
      .rr-more-weirder:hover:not(:disabled) {
        border-color: var(--rr-warm);
        color: var(--rr-warm);
      }
      .rr-more-calmer:hover:not(:disabled) {
        border-color: #059669;
        color: #059669;
      }

      /* ─── More Directions Loading ────────────────────────────── */
      .rr-more-loading {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.55rem 1.25rem;
        border: 2px solid var(--rr-accent-light);
        border-radius: 10px;
        background: var(--rr-accent-light);
      }

      .rr-more-spinner {
        width: 16px;
        height: 16px;
        border: 2px solid var(--rr-border);
        border-top-color: var(--rr-accent);
        border-radius: 50%;
        animation: rrSpin 0.8s linear infinite;
        flex-shrink: 0;
      }
      @keyframes rrSpin {
        to { transform: rotate(360deg); }
      }

      .rr-more-loading-text {
        font-family: 'Space Mono', monospace;
        font-size: 0.75rem;
        color: var(--rr-accent);
        font-weight: 700;
      }

      /* ─── Streaming Dot ─────────────────────────────────────── */
      .rr-streaming-dot {
        display: inline-block;
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: var(--rr-accent);
        margin-left: 0.35rem;
        vertical-align: middle;
        animation: rrPulse 1.2s ease-in-out infinite;
      }
      @keyframes rrPulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.4; transform: scale(0.7); }
      }

      /* ─── Loading Preview ───────────────────────────────────── */
      .rr-loading-preview {
        position: relative;
        width: 280px;
        max-width: 90%;
        border-radius: 10px;
        overflow: hidden;
        border: 2px solid var(--rr-border);
        box-shadow: 0 4px 16px rgba(28, 25, 23, 0.08);
      }
      .rr-loading-screenshot {
        width: 100%;
        height: auto;
        display: block;
        max-height: 160px;
        object-fit: cover;
        object-position: top;
      }
      .rr-loading-preview-overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(
          135deg,
          rgba(109, 40, 217, 0.08) 0%,
          rgba(250, 246, 240, 0.5) 50%,
          rgba(109, 40, 217, 0.08) 100%
        );
        animation: rrShimmer 2s ease-in-out infinite;
        background-size: 200% 200%;
      }
      @keyframes rrShimmer {
        0% { background-position: 200% 200%; }
        100% { background-position: 0% 0%; }
      }

      /* ─── Example Outputs (static, on the Astro page) ────────── */
      .rr-examples-section {
        max-width: 1100px;
        margin: 4rem auto 0;
        padding: 3rem 1.5rem 0;
        border-top: 1px solid var(--rr-border);
      }
      .rr-examples-eyebrow {
        font-family: 'Space Mono', monospace;
        font-size: 0.65rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        color: var(--rr-accent);
        text-align: center;
        margin: 0 0 0.75rem;
      }
      .rr-examples-heading {
        font-family: 'Permanent Marker', cursive;
        font-size: 1.75rem;
        text-align: center;
        color: var(--rr-ink);
        margin: 0 0 1rem;
        letter-spacing: -0.01em;
        line-height: 1.15;
      }
      @media (min-width: 640px) {
        .rr-examples-heading { font-size: 2.25rem; }
      }
      .rr-examples-intro {
        max-width: 560px;
        margin: 0 auto 2.5rem;
        font-size: 0.95rem;
        color: var(--rr-gray);
        text-align: center;
        line-height: 1.55;
      }
      .rr-examples-intro strong {
        color: var(--rr-ink);
        font-weight: 600;
      }
      .rr-example-target {
        display: block;
        text-align: center;
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        color: var(--rr-light-gray);
        margin: 0 0 1.5rem;
        letter-spacing: 0.04em;
      }
      .rr-example-target span {
        color: var(--rr-accent);
        background: var(--rr-accent-light);
        padding: 0.15rem 0.5rem;
        border-radius: 4px;
      }
      .rr-example-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1.25rem;
        margin-bottom: 2.5rem;
      }
      @media (min-width: 720px) {
        .rr-example-grid {
          grid-template-columns: repeat(3, 1fr);
          gap: 1rem;
        }
      }
      .rr-example-card {
        background: var(--rr-card-bg);
        border: 2px solid var(--rr-border);
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 4px 18px rgba(28, 25, 23, 0.06), 0 1px 3px rgba(28, 25, 23, 0.04);
        display: flex;
        flex-direction: column;
      }
      .rr-example-preview {
        margin: 0.75rem 0.75rem 0;
        border-radius: 8px;
        overflow: hidden;
        border: 1px solid var(--rr-border);
        aspect-ratio: 7 / 5;
        background: #f5f5f5;
      }
      .rr-example-preview svg {
        width: 100%;
        height: 100%;
        display: block;
      }
      .rr-example-body {
        padding: 0.85rem 1rem 1rem;
        display: flex;
        flex-direction: column;
        gap: 0.4rem;
      }
      .rr-example-num {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        font-weight: 700;
        color: var(--rr-light-gray);
        text-transform: uppercase;
        letter-spacing: 0.1em;
      }
      .rr-example-name {
        font-family: 'Playfair Display', serif;
        font-size: 1.15rem;
        font-weight: 600;
        color: var(--rr-ink);
        margin: 0;
        line-height: 1.2;
      }
      .rr-example-tagline {
        font-size: 0.85rem;
        color: var(--rr-gray);
        font-style: italic;
        margin: 0;
        line-height: 1.4;
      }
      .rr-example-palette {
        display: flex;
        gap: 4px;
        margin: 0.25rem 0 0.1rem;
      }
      .rr-example-swatch {
        width: 22px;
        height: 22px;
        border-radius: 4px;
        border: 1px solid rgba(0,0,0,0.08);
        flex-shrink: 0;
      }
      .rr-example-font {
        font-family: 'Space Mono', monospace;
        font-size: 0.68rem;
        color: var(--rr-light-gray);
        line-height: 1.45;
        margin: 0;
      }
      .rr-examples-howto {
        max-width: 560px;
        margin: 0 auto;
        padding-bottom: 1rem;
      }
      .rr-howto-title {
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--rr-light-gray);
        text-align: center;
        margin: 0 0 1.25rem;
      }
      .rr-howto-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 0.85rem;
      }
      @media (min-width: 540px) {
        .rr-howto-grid { grid-template-columns: repeat(3, 1fr); gap: 0.75rem; }
      }
      .rr-howto-step {
        background: var(--rr-card-bg);
        border: 1.5px solid var(--rr-border);
        border-radius: 10px;
        padding: 0.85rem 1rem 1rem;
      }
      .rr-howto-num {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        font-weight: 700;
        color: var(--rr-accent);
        letter-spacing: 0.12em;
        margin-bottom: 0.35rem;
      }
      .rr-howto-step h4 {
        font-family: 'Cabin', sans-serif;
        font-size: 0.95rem;
        font-weight: 700;
        color: var(--rr-ink);
        margin: 0 0 0.25rem;
      }
      .rr-howto-step p {
        font-size: 0.78rem;
        color: var(--rr-gray);
        line-height: 1.45;
        margin: 0;
      }

      /* ─── When to spin one (use cases) ──────────────────────── */
      .rr-uses-section {
        max-width: 1100px;
        margin: 4rem auto 0;
        padding: 3rem 1.5rem 0;
        border-top: 1px solid var(--rr-border);
      }
      .rr-uses-eyebrow {
        font-family: 'Space Mono', monospace;
        font-size: 0.65rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        color: var(--rr-warm);
        text-align: center;
        margin: 0 0 0.75rem;
      }
      .rr-uses-heading {
        font-family: 'Permanent Marker', cursive;
        font-size: 1.75rem;
        text-align: center;
        color: var(--rr-ink);
        margin: 0 0 1rem;
        letter-spacing: -0.01em;
        line-height: 1.15;
      }
      @media (min-width: 640px) {
        .rr-uses-heading { font-size: 2.25rem; }
      }
      .rr-uses-intro {
        max-width: 560px;
        margin: 0 auto 2.5rem;
        font-size: 0.95rem;
        color: var(--rr-gray);
        text-align: center;
        line-height: 1.55;
      }
      .rr-uses-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
      }
      @media (min-width: 640px) {
        .rr-uses-grid {
          grid-template-columns: repeat(2, 1fr);
          gap: 1.25rem;
        }
      }
      .rr-use-card {
        background: var(--rr-card-bg);
        border: 2px solid var(--rr-border);
        border-radius: 14px;
        padding: 1.25rem 1.35rem 1.4rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        position: relative;
        transition: border-color 0.15s, transform 0.15s;
      }
      .rr-use-card:hover {
        border-color: var(--rr-accent);
        transform: translateY(-2px);
      }
      .rr-use-card-icon {
        width: 36px;
        height: 36px;
        border-radius: 8px;
        background: var(--rr-accent-light);
        color: var(--rr-accent);
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: 'Permanent Marker', cursive;
        font-size: 1.1rem;
        margin-bottom: 0.25rem;
      }
      .rr-use-card-icon--warm {
        background: var(--rr-warm-light);
        color: var(--rr-warm);
      }
      .rr-use-card-when {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--rr-light-gray);
      }
      .rr-use-card-name {
        font-family: 'Playfair Display', serif;
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--rr-ink);
        margin: 0;
        line-height: 1.2;
      }
      .rr-use-card-body {
        font-size: 0.88rem;
        color: var(--rr-gray);
        line-height: 1.5;
        margin: 0;
      }
      .rr-use-card-tip {
        font-family: 'Space Mono', monospace;
        font-size: 0.7rem;
        color: var(--rr-accent);
        margin-top: 0.4rem;
        padding-top: 0.6rem;
        border-top: 1px dashed var(--rr-border);
        line-height: 1.4;
      }
      .rr-use-card-tip strong {
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.06em;
        font-size: 0.62rem;
        opacity: 0.8;
        margin-right: 0.35rem;
      }

      /* ─── Three modes panel ──────────────────────────────────── */
      .rr-modes-section {
        max-width: 1100px;
        margin: 4rem auto 0;
        padding: 3rem 1.5rem 0;
        border-top: 1px solid var(--rr-border);
      }
      .rr-modes-eyebrow {
        font-family: 'Space Mono', monospace;
        font-size: 0.65rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        color: var(--rr-accent);
        text-align: center;
        margin: 0 0 0.75rem;
      }
      .rr-modes-heading {
        font-family: 'Permanent Marker', cursive;
        font-size: 1.75rem;
        text-align: center;
        color: var(--rr-ink);
        margin: 0 0 1rem;
        letter-spacing: -0.01em;
        line-height: 1.15;
      }
      @media (min-width: 640px) {
        .rr-modes-heading { font-size: 2.25rem; }
      }
      .rr-modes-intro {
        max-width: 580px;
        margin: 0 auto 2.5rem;
        font-size: 0.95rem;
        color: var(--rr-gray);
        text-align: center;
        line-height: 1.55;
      }
      .rr-modes-intro strong {
        color: var(--rr-ink);
        font-weight: 600;
      }
      .rr-modes-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
      }
      @media (min-width: 720px) {
        .rr-modes-grid {
          grid-template-columns: repeat(3, 1fr);
          gap: 1.1rem;
        }
      }
      .rr-mode-card {
        background: var(--rr-card-bg);
        border: 2px solid var(--rr-border);
        border-radius: 14px;
        padding: 1.25rem 1.35rem 1.4rem;
        display: flex;
        flex-direction: column;
        gap: 0.6rem;
        position: relative;
        border-top-width: 6px;
        transition: transform 0.15s, border-color 0.15s, box-shadow 0.15s;
      }
      .rr-mode-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 18px rgba(28, 25, 23, 0.06);
      }
      .rr-mode-card--safe { border-top-color: #5b6c93; }
      .rr-mode-card--safe:hover { border-color: #5b6c93; }
      .rr-mode-card--designer { border-top-color: #ea580c; }
      .rr-mode-card--designer:hover { border-color: #ea580c; }
      .rr-mode-card--wild { border-top-color: #c026d3; }
      .rr-mode-card--wild:hover { border-color: #c026d3; }
      .rr-mode-head {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 0.1rem;
      }
      .rr-mode-num {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        font-weight: 700;
        color: var(--rr-light-gray);
        letter-spacing: 0.12em;
      }
      .rr-mode-tag {
        font-family: 'Space Mono', monospace;
        font-size: 0.62rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.14em;
        padding: 0.18rem 0.5rem;
        border-radius: 4px;
      }
      .rr-mode-card--safe .rr-mode-tag { background: rgba(91, 108, 147, 0.12); color: #4a5a82; }
      .rr-mode-card--designer .rr-mode-tag { background: var(--rr-warm-light); color: var(--rr-warm); }
      .rr-mode-card--wild .rr-mode-tag { background: rgba(192, 38, 211, 0.12); color: #a51eb1; }
      .rr-mode-name {
        font-family: 'Playfair Display', serif;
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--rr-ink);
        margin: 0;
        line-height: 1.2;
      }
      .rr-mode-body {
        font-size: 0.88rem;
        color: var(--rr-gray);
        line-height: 1.5;
        margin: 0;
      }
      .rr-mode-when {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
        font-size: 0.78rem;
        color: var(--rr-gray);
        line-height: 1.4;
        padding-top: 0.65rem;
        border-top: 1px dashed var(--rr-border);
      }
      .rr-mode-when-label {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--rr-light-gray);
      }
      .rr-mode-samples {
        margin-top: 0.25rem;
      }
      .rr-mode-sample-label {
        font-family: 'Space Mono', monospace;
        font-size: 0.6rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--rr-light-gray);
        display: block;
        margin-bottom: 0.4rem;
      }
      .rr-mode-sample-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 0.3rem;
      }
      .rr-mode-sample-list li {
        font-family: 'Playfair Display', serif;
        font-size: 0.88rem;
        color: var(--rr-ink);
        font-style: italic;
      }
      .rr-mode-sample-list li::before {
        content: '— ';
        color: var(--rr-light-gray);
        font-style: normal;
        margin-right: 0.15rem;
      }
      .rr-modes-foot {
        max-width: 600px;
        margin: 2rem auto 0;
        font-size: 0.82rem;
        color: var(--rr-light-gray);
        text-align: center;
        line-height: 1.5;
        font-style: italic;
      }
    </style>
  </Fragment>

  <div class="rr-page">
    <a href="#main-content" class="skip-nav">Skip to content</a>

    <header class="rr-header">
      <a href="/" class="rr-back" aria-label="Back to stanwood.dev">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
        stanwood.dev
      </a>
      <span class="rr-brand">// redesign-rolodex</span>
    </header>

    <main id="main-content" class="rr-main">
      <div class="rr-app">
        <RedesignRolodexPage client:load />
      </div>

      <section class="rr-uses-section" aria-label="When to spin a rolodex">
        <p class="rr-uses-eyebrow">when this is actually useful</p>
        <h2 class="rr-uses-heading">Four reasons to spin one.</h2>
        <p class="rr-uses-intro">
          A rolodex isn't a finished design — it's a brainstorm. Here's when it's worth your sixty seconds.
        </p>

        <div class="rr-uses-grid">
          <article class="rr-use-card">
            <div class="rr-use-card-icon">★</div>
            <span class="rr-use-card-when">stuck pitching</span>
            <h3 class="rr-use-card-name">Client wants three options.</h3>
            <p class="rr-use-card-body">
              You've got direction one. Direction two is a tweak. Spin and steal direction three from a place you'd never have looked yourself.
            </p>
            <p class="rr-use-card-tip"><strong>tip</strong>Stay in client-safe mode. The picks stay plausible enough to actually present.</p>
          </article>

          <article class="rr-use-card">
            <div class="rr-use-card-icon">▤</div>
            <span class="rr-use-card-when">pre-pitch</span>
            <h3 class="rr-use-card-name">Mood board in one minute.</h3>
            <p class="rr-use-card-body">
              Need vibe references for a kickoff? Spin a hero brand, a competitor, or just <em>example.com</em>. Drop the cards straight into your slides.
            </p>
            <p class="rr-use-card-tip"><strong>tip</strong>Hit "more" twice for nine directions across one site.</p>
          </article>

          <article class="rr-use-card">
            <div class="rr-use-card-icon rr-use-card-icon--warm">↻</div>
            <span class="rr-use-card-when">rebrand brainstorm</span>
            <h3 class="rr-use-card-name">Your own site, six universes over.</h3>
            <p class="rr-use-card-body">
              Identity feels stale. Boss says "freshen it up." Spin your own URL and see what an editorial press, an arcade, or a brutalist redesign would look like.
            </p>
            <p class="rr-use-card-tip"><strong>tip</strong>Flip to alt-timeline mode for the version no committee would approve.</p>
          </article>

          <article class="rr-use-card">
            <div class="rr-use-card-icon">⌘</div>
            <span class="rr-use-card-when">ai handoff</span>
            <h3 class="rr-use-card-name">Sketch to working draft.</h3>
            <p class="rr-use-card-body">
              Each card ships with a copy-paste prompt. Drop it into Claude, ChatGPT, or Cursor and you've got a starting layout in minutes — not hours.
            </p>
            <p class="rr-use-card-tip"><strong>tip</strong>The prompts include palette + font notes, so the first render lands close.</p>
          </article>
        </div>
      </section>

      <section class="rr-modes-section" aria-label="Three modes of weirdness">
        <p class="rr-modes-eyebrow">pick your weirdness</p>
        <h2 class="rr-modes-heading">Three dials. One spin.</h2>
        <p class="rr-modes-intro">
          Same URL, three very different stacks of cards. The mode toggle right above the URL field
          decides how far the rolodex strays from the original — <strong>pitch-deck-safe to fever-dream.</strong>
        </p>

        <div class="rr-modes-grid">
          <article class="rr-mode-card rr-mode-card--safe">
            <div class="rr-mode-head">
              <span class="rr-mode-num">01</span>
              <span class="rr-mode-tag">client-safe</span>
            </div>
            <h3 class="rr-mode-name">Polished &amp; plausible</h3>
            <p class="rr-mode-body">
              Reads like real agency comps. Familiar grids, restrained palettes, tight type pairings.
              You could screenshare these in a kickoff with no caveats.
            </p>
            <div class="rr-mode-when">
              <span class="rr-mode-when-label">when to spin</span>
              <span>real client work, three-direction pitches, conservative brands</span>
            </div>
            <div class="rr-mode-samples">
              <span class="rr-mode-sample-label">Sample directions</span>
              <ul class="rr-mode-sample-list">
                <li>Modern Editorial</li>
                <li>Minimal SaaS</li>
                <li>Coastal Boutique</li>
              </ul>
            </div>
          </article>

          <article class="rr-mode-card rr-mode-card--designer">
            <div class="rr-mode-head">
              <span class="rr-mode-num">02</span>
              <span class="rr-mode-tag">designer</span>
            </div>
            <h3 class="rr-mode-name">Daring &amp; expressive</h3>
            <p class="rr-mode-body">
              The default. Real moves, strong opinions, but still sellable.
              Editorial stretches, branded systems with personality, layouts that take a stance.
            </p>
            <div class="rr-mode-when">
              <span class="rr-mode-when-label">when to spin</span>
              <span>portfolio refreshes, side projects, anything that needs a point of view</span>
            </div>
            <div class="rr-mode-samples">
              <span class="rr-mode-sample-label">Sample directions</span>
              <ul class="rr-mode-sample-list">
                <li>Brutalist Library</li>
                <li>Botanical Press</li>
                <li>Sunset Arcade</li>
              </ul>
            </div>
          </article>

          <article class="rr-mode-card rr-mode-card--wild">
            <div class="rr-mode-head">
              <span class="rr-mode-num">03</span>
              <span class="rr-mode-tag">alt timeline</span>
            </div>
            <h3 class="rr-mode-name">Weird &amp; wonderful</h3>
            <p class="rr-mode-body">
              The version no committee would approve. Anti-design, ASCII webrings,
              Memphis grids on speed, Geocities-by-way-of-2026. Use sparingly — but use it.
            </p>
            <div class="rr-mode-when">
              <span class="rr-mode-when-label">when to spin</span>
              <span>creative warmups, art experiments, escaping a stale brief</span>
            </div>
            <div class="rr-mode-samples">
              <span class="rr-mode-sample-label">Sample directions</span>
              <ul class="rr-mode-sample-list">
                <li>Chrome Cathedral</li>
                <li>Tape Loop Bulletin</li>
                <li>Liquid Aughts</li>
              </ul>
            </div>
          </article>
        </div>

        <p class="rr-modes-foot">
          Flip the toggle before you spin — or hit "more" with a different mode to layer takes from all three on the same site.
        </p>
      </section>

      <section class="rr-examples-section" aria-label="Example rolodex outputs">
        <p class="rr-examples-eyebrow">a peek at the rolodex</p>
        <h2 class="rr-examples-heading">What a spin actually gives you.</h2>
        <p class="rr-examples-intro">
          Each spin returns <strong>three to nine design directions</strong> — every card is a fresh take on
          the same site, with a concept screenshot, a color palette, font notes, and a copy-paste prompt
          you can hand to ChatGPT, Claude, or your code editor. Below: three sample directions from a
          fictional run.
        </p>
        <p class="rr-example-target">redesigning <span>example.com</span></p>

        <div class="rr-example-grid">
          <article class="rr-example-card">
            <div class="rr-example-preview">
              <svg viewBox="0 0 280 200" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Brutalist Library concept preview">
                <rect width="280" height="200" fill="#fafaf5"/>
                <rect width="280" height="34" fill="#0a0a0a"/>
                <rect x="14" y="13" width="62" height="8" fill="#fafaf5"/>
                <rect x="194" y="13" width="50" height="8" fill="#fafaf5"/>
                <rect x="252" y="13" width="14" height="8" fill="#cc3333"/>
                <text x="14" y="80" font-family="Georgia, serif" font-size="22" fill="#0a0a0a" font-weight="700">On Reading.</text>
                <line x1="14" y1="92" x2="92" y2="92" stroke="#cc3333" stroke-width="2"/>
                <rect x="14" y="106" width="252" height="3" fill="#666"/>
                <rect x="14" y="114" width="240" height="3" fill="#666"/>
                <rect x="14" y="122" width="252" height="3" fill="#666"/>
                <rect x="14" y="130" width="180" height="3" fill="#666"/>
                <rect x="14" y="158" width="118" height="22" fill="#0a0a0a"/>
                <text x="22" y="173" font-family="Georgia, serif" font-size="9" fill="#fafaf5" letter-spacing="0.5">CONTINUE READING →</text>
              </svg>
            </div>
            <div class="rr-example-body">
              <span class="rr-example-num">Card 01</span>
              <h3 class="rr-example-name">Brutalist Library</h3>
              <p class="rr-example-tagline">Everything in service of reading. Decoration is suspect.</p>
              <div class="rr-example-palette">
                <span class="rr-example-swatch" style="background:#0a0a0a" title="#0a0a0a"></span>
                <span class="rr-example-swatch" style="background:#fafaf5" title="#fafaf5"></span>
                <span class="rr-example-swatch" style="background:#cc3333" title="#cc3333"></span>
                <span class="rr-example-swatch" style="background:#666666" title="#666666"></span>
                <span class="rr-example-swatch" style="background:#dddddd" title="#dddddd"></span>
              </div>
              <p class="rr-example-font">Stark serifs. Generous body type. No flourishes, no rounded corners.</p>
            </div>
          </article>

          <article class="rr-example-card">
            <div class="rr-example-preview">
              <svg viewBox="0 0 280 200" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Sunset Arcade concept preview">
                <defs>
                  <linearGradient id="sa-bg" x1="0" y1="0" x2="0" y2="1">
                    <stop offset="0" stop-color="#1a0033"/>
                    <stop offset="0.6" stop-color="#3a0855"/>
                    <stop offset="1" stop-color="#1a0033"/>
                  </linearGradient>
                </defs>
                <rect width="280" height="200" fill="url(#sa-bg)"/>
                <text x="14" y="62" font-family="'Courier New', monospace" font-size="28" fill="#ff00aa" font-weight="700" letter-spacing="2">RETRO</text>
                <text x="14" y="94" font-family="'Courier New', monospace" font-size="28" fill="#00f0ff" font-weight="700" letter-spacing="2">.PIXEL</text>
                <rect x="14" y="118" width="128" height="32" fill="none" stroke="#ffcc00" stroke-width="2"/>
                <text x="32" y="139" font-family="'Courier New', monospace" font-size="11" fill="#ffcc00" letter-spacing="1">[ INSERT COIN ]</text>
                <line x1="0" y1="170" x2="280" y2="170" stroke="#ff00aa" stroke-width="1" opacity="0.5"/>
                <line x1="0" y1="180" x2="280" y2="180" stroke="#00f0ff" stroke-width="1" opacity="0.5"/>
                <line x1="0" y1="190" x2="280" y2="190" stroke="#ff5c1a" stroke-width="1" opacity="0.5"/>
              </svg>
            </div>
            <div class="rr-example-body">
              <span class="rr-example-num">Card 02</span>
              <h3 class="rr-example-name">Sunset Arcade</h3>
              <p class="rr-example-tagline">Your site at 2am — neon humming, scanlines, a quarter on the cabinet.</p>
              <div class="rr-example-palette">
                <span class="rr-example-swatch" style="background:#1a0033" title="#1a0033"></span>
                <span class="rr-example-swatch" style="background:#ff00aa" title="#ff00aa"></span>
                <span class="rr-example-swatch" style="background:#00f0ff" title="#00f0ff"></span>
                <span class="rr-example-swatch" style="background:#ffcc00" title="#ffcc00"></span>
                <span class="rr-example-swatch" style="background:#ff5c1a" title="#ff5c1a"></span>
              </div>
              <p class="rr-example-font">Bold display monospace. Glowing buttons. Subtle CRT scanlines on top.</p>
            </div>
          </article>

          <article class="rr-example-card">
            <div class="rr-example-preview">
              <svg viewBox="0 0 280 200" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Botanical Press concept preview">
                <rect width="280" height="200" fill="#fdfbf6"/>
                <text x="140" y="34" font-family="Georgia, serif" font-size="9" fill="#a26d4e" text-anchor="middle" letter-spacing="3">— WINTER ISSUE —</text>
                <text x="140" y="84" font-family="Georgia, serif" font-size="36" font-style="italic" fill="#3d4f3a" text-anchor="middle">Bloom</text>
                <line x1="100" y1="98" x2="180" y2="98" stroke="#a26d4e" stroke-width="1"/>
                <text x="140" y="116" font-family="Georgia, serif" font-size="10" fill="#1c1c1c" text-anchor="middle">A quarterly almanac of slow things.</text>
                <circle cx="40" cy="160" r="14" fill="#e8d9c0"/>
                <circle cx="40" cy="160" r="7" fill="#3d4f3a" opacity="0.75"/>
                <line x1="40" y1="174" x2="40" y2="190" stroke="#3d4f3a" stroke-width="1.5"/>
                <line x1="40" y1="180" x2="32" y2="184" stroke="#3d4f3a" stroke-width="1"/>
                <line x1="40" y1="180" x2="48" y2="184" stroke="#3d4f3a" stroke-width="1"/>
                <rect x="100" y="155" width="80" height="2" fill="#1c1c1c"/>
                <rect x="100" y="162" width="60" height="2" fill="#1c1c1c"/>
                <rect x="100" y="169" width="80" height="2" fill="#1c1c1c"/>
                <rect x="100" y="176" width="40" height="2" fill="#1c1c1c"/>
                <text x="240" y="190" font-family="Georgia, serif" font-size="8" fill="#a26d4e" font-style="italic">vol. 4</text>
              </svg>
            </div>
            <div class="rr-example-body">
              <span class="rr-example-num">Card 03</span>
              <h3 class="rr-example-name">Botanical Press</h3>
              <p class="rr-example-tagline">Slow design. Pressed flowers between pages. Hand-set type.</p>
              <div class="rr-example-palette">
                <span class="rr-example-swatch" style="background:#fdfbf6" title="#fdfbf6"></span>
                <span class="rr-example-swatch" style="background:#3d4f3a" title="#3d4f3a"></span>
                <span class="rr-example-swatch" style="background:#a26d4e" title="#a26d4e"></span>
                <span class="rr-example-swatch" style="background:#e8d9c0" title="#e8d9c0"></span>
                <span class="rr-example-swatch" style="background:#1c1c1c" title="#1c1c1c"></span>
              </div>
              <p class="rr-example-font">Editorial italic serif. Warm cream paper. Hand-drawn botanical accents.</p>
            </div>
          </article>
        </div>

        <div class="rr-examples-howto">
          <p class="rr-howto-title">how to spin one</p>
          <div class="rr-howto-grid">
            <div class="rr-howto-step">
              <div class="rr-howto-num">01</div>
              <h4>Paste a URL</h4>
              <p>Any landing page, product, portfolio, or marketing site. We screenshot it.</p>
            </div>
            <div class="rr-howto-step">
              <div class="rr-howto-num">02</div>
              <h4>Pick a mode</h4>
              <p>Designer mode stays plausible. Weirdness mode goes off the rails.</p>
            </div>
            <div class="rr-howto-step">
              <div class="rr-howto-num">03</div>
              <h4>Copy the prompt</h4>
              <p>Each card has a ready-made prompt to paste into your favorite AI assistant.</p>
            </div>
          </div>
        </div>
      </section>
    </main>
  </div>
</BaseLayout>
